// ┌──────────────────────────────────────────────────────────┐
// │  BUBO SKIN TEMPLATE                                     │
// │                                                          │
// │  Copy this file, rename it to YourSkinName.swift,        │
// │  fill in the values below, and register your skin        │
// │  in SkinCatalog.allSkins (SkinDefinition.swift).         │
// │                                                          │
// │  See CONTRIBUTING_SKINS.md for full instructions.        │
// └──────────────────────────────────────────────────────────┘

import SwiftUI

// MARK: - Your Skin Name
// Author: Your Name (@github_handle)
// Brief description of the skin's visual identity.

/*
extension SkinCatalog {
    static let yourSkinName = SkinDefinition(
        // A unique, stable ID (lowercase_snake_case). Never change after release.
        id: "your_skin_name",

        // Name shown in the Settings picker.
        displayName: "Your Skin Name",

        // Your name or GitHub handle.
        author: "@your_github",

        // Primary accent color — used for buttons, highlights, tint, accent bars.
        accentColor: Color(red: 0.0, green: 0.5, blue: 1.0),

        // Subtle color overlaid on all surfaces. Keep it very dark/muted.
        surfaceTint: Color(red: 0.0, green: 0.02, blue: 0.08),

        // Opacity of the surface tint (0 = invisible, 0.2–0.4 = typical).
        surfaceTintOpacity: 0.3,

        // Background gradient — the ambient glow behind the UI.
        backgroundGradient: SkinGradient(
            colors: [
                Color(red: 0.0, green: 0.3, blue: 0.6).opacity(0.20),
                Color(red: 0.1, green: 0.0, blue: 0.4).opacity(0.10),
                .clear,
            ],
            // Use .linear or .radial:
            //   .linear(startPoint: .topLeading, endPoint: .bottomTrailing)
            //   .radial(center: .top, startRadius: 0, endRadius: 500)
            style: .linear(startPoint: .topLeading, endPoint: .bottomTrailing)
        ),

        // 1–2 colors for the skin picker thumbnail.
        previewColors: [
            Color(red: 0.0, green: 0.5, blue: 1.0),
            Color(red: 0.3, green: 0.0, blue: 0.6),
        ],

        // Set true if your skin has a dark/moody vibe.
        prefersDarkTint: false,

        // Optional: secondary accent for button gradients. Omit to auto-derive.
        // secondaryAccent: Color(red: 0.2, green: 0.0, blue: 0.5),

        // Button fill style: .gradient (default), .solid, or .glass
        buttonStyle: .gradient
    )
}
*/
